@model List<CommissionReportRow>
@{
    ViewData["Title"] = "Commission Report";
    var fromDate = ViewBag.FromDate as DateTime?;
    var toDate = ViewBag.ToDate as DateTime?;
}

<div class="container-fluid">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
            <li class="breadcrumb-item"><a asp-action="Dashboard">Financial Dashboard</a></li>
            <li class="breadcrumb-item active" aria-current="page">Commission Report</li>
        </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="bi bi-percent"></i> Commission Report</h1>
    </div>

    <div class="card mb-4">
        <div class="card-body">
            <form method="get" class="row g-3 align-items-end">
                <div class="col-md-4">
                    <label class="form-label">From Date</label>
                    <input type="date" name="fromDate" class="form-control" value="@fromDate?.ToString("yyyy-MM-dd")" />
                </div>
                <div class="col-md-4">
                    <label class="form-label">To Date</label>
                    <input type="date" name="toDate" class="form-control" value="@toDate?.ToString("yyyy-MM-dd")" />
                </div>
                <div class="col-md-4">
                    <button type="submit" class="btn btn-primary"><i class="bi bi-filter"></i> Filter</button>
                    <a asp-action="CommissionReport" class="btn btn-outline-secondary"><i class="bi bi-x-circle"></i> Clear</a>
                </div>
            </form>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-md-6">
            <div class="card bg-info text-white">
                <div class="card-body">
                    <h5>Total Commission Collected</h5>
                    <h3>$@Model.Sum(r => r.TotalCommission).ToString("N2")</h3>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card bg-primary text-white">
                <div class="card-body">
                    <h5>Total Orders Processed</h5>
                    <h3>@Model.Sum(r => r.OrderCount)</h3>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>Period</th>
                            <th>Date</th>
                            <th>Orders</th>
                            <th>Total Commission</th>
                            <th>Avg Commission/Order</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Count == 0)
                        {
                            <tr>
                                <td colspan="5" class="text-center text-muted py-4">
                                    <i class="bi bi-inbox fs-3 d-block mb-2"></i>
                                    No commission data found.
                                </td>
                            </tr>
                        }
                        @foreach (var row in Model)
                        {
                            <tr>
                                <td>@row.Period</td>
                                <td>@row.Date.ToString("MMM dd, yyyy")</td>
                                <td>@row.OrderCount</td>
                                <td>$@row.TotalCommission.ToString("N2")</td>
                                <td>$@row.AverageCommission.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr class="fw-bold">
                            <td colspan="2">TOTAL</td>
                            <td>@Model.Sum(r => r.OrderCount)</td>
                            <td>$@Model.Sum(r => r.TotalCommission).ToString("N2")</td>
                            <td>$@(Model.Count > 0 ? Model.Average(r => r.TotalCommission).ToString("N2") : "0.00")</td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>